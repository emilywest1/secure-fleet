#  http://10.175.224.3/nagios4/
- name: Install and configure Nagios
  hosts: nagios
  become: true
  gather_facts: true

  vars:
    nagios_admin_password: "nagiosadmin"
    nagios_cfg_dir: /etc/nagios4
    nagios_objects_dir: /etc/nagios4/conf.d

  tasks:

    - name: Install Nagios and required packages
      ansible.builtin.apt:
        name:
          - nagios4
          - nagios-plugins-basic
          - nagios-plugins-standard
          - nagios4-core
          - libwww-perl
        state: present
        update_cache: true

    - name: Install apache2
      ansible.builtin.apt:
        name:
          - apache2
          - apache2-utils
        state: present

    - name: Set Nagios web UI admin password (digest)
      ansible.builtin.shell: >
        htdigest -b -c /etc/nagios4/htdigest.users "Nagios4" nagiosadmin {{ nagios_admin_password }}

    - name: Deploy Nagios node host configs
      ansible.builtin.template:
        src: nagios_nodes.cfg.j2
        dest: "{{ nagios_objects_dir }}/nagios_nodes.cfg"
        owner: root
        group: root
        mode: "0644"
      notify: Restart Nagios

    - name: Enable Apache CGI module
      ansible.builtin.command: a2enmod cgi
      notify: Restart Apache

    - name: Enable Nagios Apache config
      ansible.builtin.command: a2enconf nagios4-cgi
      notify: Restart Apache
      failed_when: false

    - name: Set Nagios to use basic auth
      ansible.builtin.replace:
        path: /etc/apache2/conf-available/nagios4-cgi.conf
        regexp: 'AuthType Digest'
        replace: 'AuthType Basic'
      notify: Restart Apache

    - name: Point to htpasswd file
      ansible.builtin.replace:
        path: /etc/apache2/conf-available/nagios4-cgi.conf
        regexp: 'AuthUserFile\s+.*'
        replace: 'AuthUserFile /etc/nagios4/htpasswd.users'
      notify: Restart Apache

    - name: Validate Nagios configuration
      ansible.builtin.command: nagios4 -v /etc/nagios4/nagios.cfg
      register: nagios_verify
      changed_when: false
      failed_when: "'Total Errors:   0' not in nagios_verify.stdout"

    - name: Enable and start Nagios
      ansible.builtin.service:
        name: nagios4
        enabled: true
        state: started

    - name: Restart Apache
      ansible.builtin.service:
        name: apache2
        state: restarted

    - name: Enable and start Apache
      ansible.builtin.service:
        name: apache2
        enabled: true
        state: started

    - name: Report Nagios status
      ansible.builtin.debug:
        msg: "Nagios is running. Access the web UI at http://{{ ansible_host }}/nagios4"

  handlers:
    - name: Restart Nagios
      ansible.builtin.service:
        name: nagios4
        state: restarted

    - name: Restart Apache
      ansible.builtin.service:
        name: apache2
        state: restarted
